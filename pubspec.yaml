name: window_close_guard
description: "Flutter package for safe window close handling on desktop with confirmation dialog support."
version: 1.0.0
homepage: https://github.com/RaulCatalinas/WindowCloseGuard
repository: https://github.com/RaulCatalinas/WindowCloseGuard

environment:
  sdk: ">=3.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  window_manager: ^0.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

platforms:
  windows:
  macos:
  linux:
